import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#987654",
      light: "grey",
    },
    secondary: {
      main: "#111235",
    },
    otherColor: {
      main: "#234567",
    },
  },
});
